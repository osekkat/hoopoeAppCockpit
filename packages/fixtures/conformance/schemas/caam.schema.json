{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hoopoe.local/conformance/caam.schema.json",
  "title": "CAAM normalized adapter output",
  "type": "object",
  "required": ["tool", "source", "state", "exit", "argv", "capabilities", "payload"],
  "properties": {
    "tool": { "const": "caam" },
    "source": { "enum": ["fixture", "local"] },
    "state": { "type": "string" },
    "exit": { "type": "integer" },
    "argv": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
    "capabilities": { "type": "object" },
    "payload": {
      "type": "object",
      "required": ["commandAccepted", "accounts", "stderrText"],
      "properties": {
        "commandAccepted": { "type": "boolean" },
        "accounts": {
          "anyOf": [
            { "type": "array" },
            { "type": "null" }
          ]
        },
        "stderrText": { "type": "string" }
      }
    }
  }
}
