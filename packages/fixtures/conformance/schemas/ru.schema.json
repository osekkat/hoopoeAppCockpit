{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hoopoe.local/conformance/ru.schema.json",
  "title": "ru normalized adapter output",
  "type": "object",
  "required": ["tool", "source", "state", "exit", "argv", "capabilities", "payload"],
  "properties": {
    "tool": { "const": "ru" },
    "source": { "enum": ["fixture", "local"] },
    "state": { "type": "string" },
    "exit": { "type": "integer" },
    "argv": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
    "capabilities": { "type": "object" },
    "payload": {
      "type": "object",
      "required": ["schemaVersion", "commands", "envelope"],
      "properties": {
        "schemaVersion": { "type": "string", "minLength": 1 },
        "commands": { "type": "object" },
        "envelope": {
          "anyOf": [
            { "type": "object" },
            { "type": "null" }
          ]
        }
      }
    }
  }
}
