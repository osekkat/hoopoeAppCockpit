{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hoopoe.local/conformance/pt.schema.json",
  "title": "pt normalized adapter output",
  "type": "object",
  "required": ["tool", "source", "state", "exit", "argv", "capabilities", "payload"],
  "properties": {
    "tool": { "const": "pt" },
    "source": { "enum": ["fixture", "local"] },
    "state": { "type": "string" },
    "exit": { "type": "integer" },
    "argv": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
    "capabilities": { "type": "object" },
    "payload": {
      "type": "object",
      "required": ["commandAccepted"],
      "properties": {
        "commandAccepted": { "type": "boolean" },
        "stdoutJson": {},
        "stdoutText": { "type": "string" },
        "stderrText": { "type": "string" }
      }
    }
  }
}
