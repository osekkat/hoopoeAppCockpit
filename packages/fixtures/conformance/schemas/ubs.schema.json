{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hoopoe.local/conformance/ubs.schema.json",
  "title": "ubs normalized adapter output",
  "type": "object",
  "required": ["tool", "source", "state", "exit", "argv", "capabilities", "payload"],
  "properties": {
    "tool": { "const": "ubs" },
    "source": { "enum": ["fixture", "local"] },
    "state": { "type": "string" },
    "exit": { "type": "integer" },
    "argv": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
    "capabilities": { "type": "object" },
    "payload": {
      "type": "object",
      "required": ["commandAccepted", "helpBanner", "stderrText"],
      "properties": {
        "commandAccepted": { "type": "boolean" },
        "helpBanner": { "type": "boolean" },
        "stderrText": { "type": "string" }
      }
    }
  }
}
