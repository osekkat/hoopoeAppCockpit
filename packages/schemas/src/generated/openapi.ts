/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/v1/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness + readiness probe.
         * @description Used by the desktop FSM and by load balancers / SSH-tunnel keepalives.
         *     Always returns 200 if the daemon process can serve HTTP. Reports tool /
         *     adapter health *without* triggering full capability checks; for capability
         *     truth use `/v1/capabilities`.
         */
        get: operations["getHealth"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/version": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Daemon build version, commit, and build metadata.
         * @description Reported in `compatibility` + included on every error envelope so a renderer
         *     can correlate failures with a specific binary. Includes signing/SBOM digests
         *     recorded at install time (`plan.md §11`).
         */
        get: operations["getVersion"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/compatibility": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Daemon API version, minimum desktop version, event schema versions, migration state.
         * @description Authoritative source for "can this desktop build talk to this daemon build."
         *     Returns `migrationState` so the desktop can refuse writes during a daemon
         *     migration (§10.3). Embeds a capability snapshot at the same point-in-time so
         *     clients can correlate API-version skew with capability availability without
         *     a second round-trip.
         */
        get: operations["getCompatibility"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/capabilities": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Capability registry snapshot — every adapter, every capability ID.
         * @description Per `plan.md §2.8`: the desktop never assumes a feature exists because a tool
         *     is installed. UI buttons, stage routes, and tending jobs all gate on the IDs
         *     returned here. A `degraded` capability is rendered with its `fallback`
         *     explanation; a `missing` capability is rendered as unavailable + diagnostic.
         *
         *     Adapter contract tests (§18.3) assert capabilities, not just parser success.
         */
        get: operations["getCapabilities"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * @description RFC 7807 problem+json. `type` is a daemon-stable URN; `code` is a short
         *     machine-readable identifier (e.g., `auth.token_expired`,
         *     `capability.missing`). `instance` is a per-incident URI for support
         *     deep-links into Diagnostics.
         */
        Problem: {
            /**
             * Format: uri-reference
             * @description Stable problem-type URN (e.g., `urn:hoopoe:auth/token-expired`).
             */
            type: string;
            /** @description Short, human-readable summary. */
            title: string;
            /** @description Mirrors HTTP status. */
            status: number;
            /** @description Machine-readable error code (dot-separated namespace). */
            code: string;
            /** @description Human-readable explanation; redacted for sensitive fields. */
            detail?: string;
            /**
             * Format: uri-reference
             * @description Per-incident URI; `urn:hoopoe:incident:<id>` is canonical.
             */
            instance?: string;
            /** @description Echoes `X-Request-Id` if provided. */
            requestId?: string;
            /** @description Opaque correlation handle (matches audit-log entries). */
            correlationId?: string;
            /** @description True if the client may safely retry with the same Idempotency-Key. */
            retryable?: boolean;
            /** @description Optional hint for `Retry-After`. */
            retryAfterSeconds?: number;
            /**
             * @description For `capability.missing` / `capability.degraded` problems, the
             *     capability ID that the operation required.
             */
            capability?: string;
            /** @description For `approval.required` problems, the queued approval ID. */
            approvalId?: string;
        };
        /** @description Monotonically increasing schema version for a persisted shape. */
        SchemaVersion: number;
        /**
         * @description Opaque pagination cursor. Use the `next` cursor from a list response
         *     verbatim; do not parse it. Cursors are stable for ≥24h.
         */
        Cursor: string;
        PageMeta: {
            hasMore: boolean;
            next?: components["schemas"]["Cursor"];
            /** @description Optional total count; expensive list endpoints omit this. */
            total?: number;
        };
        /** @description Who initiated an action — used in audit + event envelopes. */
        Actor: {
            /** @enum {string} */
            kind: "user" | "agent" | "scheduler" | "system" | "dcg" | "approval";
            /** @description Stable identifier within the actor's namespace. */
            id?: string;
            /** @description Optional UI-only label; never used for authorization. */
            displayName?: string;
        };
        HealthResponse: {
            /** @enum {string} */
            status: "ok" | "degraded" | "draining";
            /** @description Stable daemon instance ID; changes on reinstall. */
            daemonId: string;
            /** Format: date-time */
            time: string;
            uptimeSeconds?: number;
            /**
             * @description Coarse per-tool adapter status (cheap; no full capability check).
             *     For authoritative capability gating call `/v1/capabilities`.
             */
            adapters?: {
                [key: string]: "ok" | "degraded" | "missing" | "unknown";
            };
        };
        VersionResponse: {
            schemaVersion: components["schemas"]["SchemaVersion"];
            daemon: {
                /** @description Semver of the daemon binary */
                version: string;
                /** @description Git SHA of the build */
                commit?: string;
                /** Format: date-time */
                buildTime?: string;
                /** @enum {string} */
                channel?: "stable" | "nightly" | "dev";
                /** @description Distribution checksum (matches release manifest) */
                checksum?: string;
                /** @description SBOM digest recorded at install time (§11) */
                sbomDigest?: string;
                /** @description Signer identity (e.g., "Hoopoe v1 Release Key") */
                signingIdentity?: string;
                /** Format: uri-reference */
                attestationUri?: string;
            };
        };
        CompatibilityReport: {
            schemaVersion: components["schemas"]["SchemaVersion"];
            /** @description Semver of the OpenAPI contract this daemon implements. */
            daemonApiVersion: string;
            /** @description Minimum compatible desktop semver. Older desktops are warned. */
            minDesktopVersion: string;
            /** @description Per-channel event schemaVersion (channel → schemaVersion int). */
            eventSchemaVersions: {
                [key: string]: number;
            };
            /**
             * @description Structured migration state per §10.3. Desktop refuses write actions
             *     while `phase` is `running` or `failed`; shows read-only Diagnostics
             *     until `idle` is reported.
             */
            migrationState: components["schemas"]["MigrationState"];
            /**
             * @description Free-form human-readable warnings (e.g., "desktop 0.0.5 below
             *     minimum 0.1.0; please update"). Renderer surfaces these in the
             *     top-bar.
             */
            unsupportedClientWarnings?: string[];
            /** @description Capability snapshot at the same point-in-time. */
            capabilities: components["schemas"]["CapabilityRegistry"];
        };
        /**
         * @description Structured daemon-migration state (§10.3). The renderer can pick the
         *     coarse `phase` enum for chrome state OR the detailed
         *     `schemaVersion`/`appliedAt`/`pending` fields for Diagnostics ("schema 7
         *     of 9 applied; pending: foo, bar"). Both stay consistent.
         */
        MigrationState: {
            /** @description Currently applied schema version. */
            schemaVersion: number;
            /**
             * Format: date-time
             * @description When the most recent migration step finished.
             */
            appliedAt: string;
            /** @description Identifiers of unapplied migrations (empty in steady state). */
            pending: string[];
            /**
             * @description Optional coarse phase. Always derivable from the structured fields
             *     (`pending == [] → idle` etc.); included so renderer chrome can pick
             *     it without computing.
             * @enum {string}
             */
            phase?: "idle" | "running" | "failed" | "rolled_back";
        };
        /**
         * @description Closed enum of tools the capability registry knows about. New
         *     ecosystem health probes (e.g., `health_zig`) require an explicit
         *     spec bump per §10.3; the daemon-side checker uses a `health_*`
         *     prefix-match for forward-compat input validation.
         * @enum {string}
         */
        ToolId: "ntm" | "br" | "bv" | "agent_mail" | "git" | "ru" | "caam" | "caut" | "dcg" | "casr" | "pt" | "srp" | "sbh" | "ubs" | "jsm" | "jfp" | "oracle" | "rch" | "health_ts" | "health_py" | "health_rs" | "health_go" | "health_generic";
        /**
         * @description - `ok` — capability satisfied a contract test.
         *     - `degraded` — works via fallback transport / partial output.
         *     - `missing` — tool reports the capability is absent.
         *     - `blocked-by-policy` — present but disallowed by config.
         *     - `untested` — daemon could not probe in this snapshot; renderer
         *       buckets with `missing` (unavailable) but Diagnostics distinguishes
         *       the two.
         * @enum {string}
         */
        CapabilityStatus: "ok" | "degraded" | "missing" | "blocked-by-policy" | "untested";
        /**
         * @description One capability slot inside a `ToolReport`. The map key is the
         *     capability ID (e.g., `ntm.sessions.list`); the value is this object.
         */
        Capability: {
            status: components["schemas"]["CapabilityStatus"];
            /**
             * @description Human-readable fallback strategy when status != `ok`. Example:
             *     `"tmux capture last"` for `ntm.panes.stream` missing.
             */
            fallback?: string;
            /**
             * @description Transport actually being used for this capability when applicable.
             * @enum {string}
             */
            transport?: "websocket" | "sse" | "http" | "stdio" | "fixture";
            /**
             * @description Adapter free-text. Surfaced in Diagnostics. Not redacted at the
             *     field name (redaction happens at the audit/log boundary, hp-je1p);
             *     do not put secrets here.
             */
            notes?: string;
        };
        /**
         * @description One tool's slice of the capability registry. Compose into
         *     `CapabilityRegistry.tools` keyed by ToolId.
         */
        ToolReport: {
            tool: components["schemas"]["ToolId"];
            /** @description Semver (or vendor version string) of the tool. Empty if unknown. */
            version: string;
            /**
             * @description How capabilities were detected. Examples: `"ntm serve"`,
             *     `"--robot-* JSON"`, `"CLI"`, `"fixture"`.
             */
            source: string;
            /**
             * @description Map of capability ID → capability slot. Capability IDs are dotted
             *     namespaces (e.g., `ntm.sessions.list`, `bv.robot.triage`). The full
             *     list of recognised capability IDs is owned by hp-r33.
             */
            capabilities: {
                [key: string]: components["schemas"]["Capability"];
            };
            /** Format: date-time */
            lastCheckedAt: string;
            /** @description Phase 0 fixture tag this registry was validated against. */
            fixturesVersion: string;
        };
        /**
         * @description Per-feature / per-tending-job degraded-mode contract (§2.8, §8.2). Used
         *     to decide whether to block, run read-only, or warn when a required
         *     capability is missing. Field names are camelCase (matching the rest of
         *     the spec); enum values are snake_case (they are tokens, not keys).
         */
        DegradedModePolicy: {
            /** @enum {string} */
            ifMissingRequired: "block_job" | "run_read_only" | "emit_diagnostic";
            /** @enum {string} */
            ifMissingOptional: "continue_with_warning" | "suppress_related_detections";
            /** @enum {string} */
            activityBehavior: "silent" | "diagnostics_only" | "activity_panel_warning";
        };
        /**
         * @description Authoritative capability snapshot returned by `/v1/capabilities`. The
         *     `tools` map is keyed by ToolId so renderer code can do
         *     `registry.tools.git.capabilities['git.push']` in O(1) without scanning
         *     an array.
         */
        CapabilityRegistry: {
            schemaVersion: components["schemas"]["SchemaVersion"];
            /**
             * Format: date-time
             * @description When this composite was assembled (single timestamp across all tools).
             */
            snapshotAt: string;
            /** @description Cross-reference with `/v1/compatibility.daemonApiVersion`. */
            daemonApiVersion: string;
            /**
             * @description Dominant fixtures tag for the snapshot. Per-tool tags can diverge
             *     (each `ToolReport.fixturesVersion` is authoritative for that tool);
             *     this is the most-common tag, used for top-level Diagnostics labels.
             */
            fixturesVersion: string;
            /**
             * @description Map of ToolId → ToolReport. OpenAPI cannot fully constrain object
             *     keys to a closed enum; the daemon enforces `ToolId` membership at
             *     serialization. Missing tools are absent from the map (not present
             *     with `null`).
             */
            tools: {
                [key: string]: components["schemas"]["ToolReport"];
            };
        };
    };
    responses: {
        /** @description RFC 7807 problem+json error envelope. */
        Problem: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
    };
    parameters: {
        /**
         * @description Stable client-generated key (ULID/UUID) for safe retries. The daemon
         *     dedupes by key within a sliding window (default 24h) and replays the
         *     original status + body. Required on retryable writes; clients that omit
         *     it on a write that turns out to be retryable will receive a
         *     `precondition-failed` problem on the second attempt.
         */
        IdempotencyKey: string;
        /** @description Client-supplied request ID; echoed on responses + audit entries. */
        RequestId: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getHealth: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Daemon is up. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthResponse"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    getVersion: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Version info. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["VersionResponse"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    getCompatibility: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Compatibility info. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompatibilityReport"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    getCapabilities: {
        parameters: {
            query?: {
                /** @description Filter to a single tool (e.g., `ntm`, `br`, `bv`). */
                tool?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Capability snapshot. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CapabilityRegistry"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
}
