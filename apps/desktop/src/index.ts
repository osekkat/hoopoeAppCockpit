// Entry point placeholder. The real Electron `main.ts` lands as decomposed
// modules under `src/main/` in hp-zir, with vendored t3code lifecycle code
// in `src/vendored/t3code/` from hp-15s. For now this file just exposes a
// version constant so the workspace has something to type-check.

export const HOOPOE_DESKTOP_PACKAGE_NAME = "@hoopoe/desktop";
export const HOOPOE_DESKTOP_VERSION = "0.0.0";
