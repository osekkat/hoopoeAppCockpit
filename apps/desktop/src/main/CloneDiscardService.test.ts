// Move-redirect placeholder. The canonical test suite lives at
// `apps/desktop/electron/clone/CloneDiscardService.test.ts` next to its
// implementation. Originally added in commit b50aced [hp-58wp]; hp-7fa
// corrected the prior "never been committed" wording without deleting
// (deletion is gated on explicit user permission per AGENTS.md RULE 1).

export {};
